<h1> <%= @email.object %> </h1>
<p> <%= @email.body %> </p>
<button> <%= link_to 'Supprimer', emails_path(email.id), method: :delete, remote: true %> </button>
<button> <%= link_to 'Marquer comme non-lu', emails_path %> </button>