console.log('Creation and rendering of a new random email through AJAX, using a Ruby Rails partial');
document.getElementsByClassName('emails__objects')[0].insertAdjacentHTML("beforeend", "<%= j render 'email', email: @email %>");